
  # High-Fidelity UI Prototype

  This is a code bundle for High-Fidelity UI Prototype. The original project is available at https://www.figma.com/design/nBoQtP5KAiIXrbrDHKJTKP/High-Fidelity-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  